import styled from "styled-components";

const GameAlert = () => {
  return <GameAlertWrapper>GameAlert</GameAlertWrapper>;
};

export default GameAlert;

const GameAlertWrapper = styled.section`
  background-color: goldenrod;
  height: 2rem;
`;
